# @package _global_

env:
  id: kinder/Obstruction2D-o2-v0
  model_name: obstruction2d
  model_kwargs:
    num_obstructions: 2

encoder:
  training_planning_timeout: 10.0
  vocabulary_size: 14

run:
  vocab_file: artifacts/encoder_o2/encoder_vocab_filtered_all_filtered.pkl

output_dir: artifacts/encoder_o2
