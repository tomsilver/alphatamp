"""TTD evaluation and experiment tooling (spec §12.1 ``ttd_eval``).

Chunk 3 populates this subpackage with :mod:`.calibrate` (the §5.3 op-cost model) and
:mod:`.pre_pilot` (the §10.0 pre-pilot: the first go/no-go gate that measures the η
landscape before any generator or learning code is written). Metrics and frontier plots
(§11) arrive with later chunks.
"""
