# SPECTRE — Running EDA / Experiment Log (moved)

This log is now chaptered by era under **[`notebook/`](notebook/README.md)**, using the same
chapter boundaries as the decision log.

Start at [`notebook/README.md`](notebook/README.md) — entry format, the full ledger, the
by-track index, and a **legacy date → entry** table resolving older `` `notebook.md` <date> ``
citations. **This log is where retracted numbers live**; check an entry's status strip before
quoting any figure.

The pre-split file is frozen byte-for-byte at
[`archive/notebook_2026-07-29_monolithic.md`](archive/notebook_2026-07-29_monolithic.md).
